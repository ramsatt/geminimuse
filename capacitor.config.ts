import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.codingtamilan.geminimuse',
  appName: 'Gemini Muse',
  webDir: 'dist/gemini-image-prompts/browser'
};

export default config;
